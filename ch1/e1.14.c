#include <stdio.h>

int main(){
//print a histogram of the frequencies of different characters in its input

   int c, i, n;
   int wordc[], countc[];
   
   n = 0;   
   while((c = getchar()) != EOF) {
      for (i = 0; i < n; ++i)
          if (c == wordc[i] {
             ++countc[i];
          else  if (c != wordc[i]) {
             ++n;
             let wordc[i] = c
            //let ++countc[i]
            }
	 else if ( c != wordc[i] {
            let ++countc[i]         
   }

}
